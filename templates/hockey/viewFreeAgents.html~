{% extends "base.html" %}
{% load i18n %}
  {% block content %}
<html>
<head>
</head>
<body>
  <table class="table table-striped">
    <thead>
      <tr>
	<th>Select</th>
	<th>Position</th>
	<th>Name</th>
	<th>Age</th>
	<th>Level</th>
      </tr>
    </thead>
      <tbody>
	{% for player in {{ FA_list }} %}
	<td> 
	  <button class="btn btn-primary" href="/players/{{
						player.id }}"> View </button>
	</td>
	<td>{{ player.position }}</td>
	<td>{{ player.name }}</td>
	<td>{{ player.age }}</td>
	<td>{{ player.level }}</td>
      </tbody>
    </table>  
  {% endfor %}
</body>
</html>

{% endblock %}
